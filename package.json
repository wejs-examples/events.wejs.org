{
  "name": "we-project-conference",
  "version": "0.3.0",
  "private": true,
  "main": "./app.js",
  "scripts": {
    "dev": "PORT=4000 node app.js",
    "verbose": "PORT=4000 LOG_LV=verbose node app.js",
    "prod": "PORT=4000 NODE_ENV=prod node app.js",
    "deps": "npm install && bower install",
    "start": "grunt && node app.js",
    "build": "grunt build",
    "test": "NODE_ENV=test LOG_LV=info ./node_modules/.bin/mocha test/bootstrap.js test/**/*.test.js",
    "prepublish": "npm prune"
  },
  "dependencies": {
    "async": "^0.9.0",
    "lodash": "^3.3.1",
    "we-core": "^0.3.30",
    "we-plugin-conference": "^0.3.1",
    "we-plugin-file": "^0.3.1",
    "we-plugin-form": "^0.3.1",
    "we-plugin-page": "^0.3.0",
    "we-theme-admin-default": "0.3.3",
    "we-theme-conference": "0.3.0",
    "we-theme-site-wejs": "^0.3.12"
  },
  "devDependencies": {
    "mocha": "^2.2.5",
    "we-test-tools": "^0.3.4",
    "sinon": "^1.14.1",
    "supertest": "^1.0.1"
  }
}
